/** @type {import('next').NextConfig} */
const nextConfig = {
     reactStrictMode: true,
     output: "standalone",
     images: {
          // domains: ["img.shields.io", "next/images"],
     },
};

export default nextConfig;
